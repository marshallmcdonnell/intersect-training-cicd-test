name: Code Checks
on: push
jobs:

  test-python-3-10:
    name: Check Python $ on Ubuntu
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]
    steps:
      - uses: actions/checkout@v3

      - name: Setup Python $
        uses: actions/setup-python@v4
        with:
          python-version: $

      - name: Install package
        run: python -m pip install -e .[test]

      - name: Test package
        run: python -m pytest
